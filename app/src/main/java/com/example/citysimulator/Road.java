package com.example.citysimulator;

// sub class
public class Road extends Structure
{
    // constructor which takes in a drawableId and label
    public Road(int drawableId, String label) {
        super(drawableId, label);
    }
}
